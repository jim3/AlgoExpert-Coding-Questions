```
// --------------------------------------------------------------------------------------------------
// Write a function that takes in a non-empty array of distinct integets and an integer representing
// a target sum. If any two numbersin  the input array sum up to the target sum, the function should
// return them in an array, in any order. Otherwise the function should return empty array. Note
// that the target  sum has to ob obtained by summing two different integers, you can't add a single
// integer to its self.
// --------------------------------------------------------------------------------------------------
```
